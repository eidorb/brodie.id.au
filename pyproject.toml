[tool.poetry]
name = "brodie.srht.site"
version = "0.1.0"
description = "My website."
authors = []

[tool.poetry.dependencies]
python = "^3.8"
Nikola = {extras = ["extras"], version = "^8.1.3"}
scipy = "^1.7.1"
matplotlib = "^3.4.2"
numpy = "^1.21.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
